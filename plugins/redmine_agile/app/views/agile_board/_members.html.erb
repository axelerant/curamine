<div class="project-members">
  <h3><%=l(:label_member_plural)%></h3>
    <% @project.users.each do |user| %>
      <span class="assignable-user" data-id="<%= user.id %>"><%= avatar(user, :size => "14").to_s.html_safe + " " + link_to_user(user) %>
      <br>
      </span>
    <% end %>
  </ul>
</div>
